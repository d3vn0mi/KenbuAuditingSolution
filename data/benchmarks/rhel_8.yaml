benchmark:
  name: "CIS Red Hat Enterprise Linux 8 Benchmark"
  version: "3.0.0"
  platform: "rhel-8"
  release_date: "2024-01-15"
  description: "Security configuration benchmark for Red Hat Enterprise Linux 8"
  url: "https://www.cisecurity.org/benchmark/red_hat_linux"

sections:
  # ===========================================================================
  # Section 1: Initial Setup
  # ===========================================================================
  - number: "1"
    title: "Initial Setup"
    children:
      - number: "1.1"
        title: "Filesystem Configuration"
        checks:
          - number: "1.1.1"
            title: "Ensure mounting of cramfs filesystems is disabled"
            description: "The cramfs filesystem type is a compressed read-only Linux filesystem embedded in small footprint systems."
            rationale: "Removing support for unneeded filesystem types reduces the local attack surface of the system. If this filesystem type is not needed, disable it."
            level: 1
            scored: true
            audit_command: |
              modprobe -n -v cramfs 2>&1 | grep -E '(install|true)'; lsmod | grep cramfs
            expected_output: |
              install /bin/true
            remediation: |
              echo "install cramfs /bin/true" >> /etc/modprobe.d/cramfs.conf
              echo "blacklist cramfs" >> /etc/modprobe.d/cramfs.conf
              rmmod cramfs 2>/dev/null

          - number: "1.1.2"
            title: "Ensure mounting of squashfs filesystems is disabled"
            description: "The squashfs filesystem type is a compressed read-only Linux filesystem embedded in small footprint systems."
            rationale: "Removing support for unneeded filesystem types reduces the local attack surface of the system."
            level: 2
            scored: true
            audit_command: |
              modprobe -n -v squashfs 2>&1 | grep -E '(install|true)'; lsmod | grep squashfs
            expected_output: |
              install /bin/true
            remediation: |
              echo "install squashfs /bin/true" >> /etc/modprobe.d/squashfs.conf
              echo "blacklist squashfs" >> /etc/modprobe.d/squashfs.conf
              rmmod squashfs 2>/dev/null

          - number: "1.1.3"
            title: "Ensure mounting of udf filesystems is disabled"
            description: "The udf filesystem type is the universal disk format used to implement ISO/IEC 13346 and ECMA-167 based file systems."
            rationale: "Removing support for unneeded filesystem types reduces the local attack surface of the system."
            level: 1
            scored: true
            audit_command: |
              modprobe -n -v udf 2>&1 | grep -E '(install|true)'; lsmod | grep udf
            expected_output: |
              install /bin/true
            remediation: |
              echo "install udf /bin/true" >> /etc/modprobe.d/udf.conf
              echo "blacklist udf" >> /etc/modprobe.d/udf.conf
              rmmod udf 2>/dev/null

          - number: "1.1.4"
            title: "Ensure /tmp is configured as a separate partition"
            description: "The /tmp directory is a world-writable directory used for temporary storage by all users and some applications."
            rationale: "Since the /tmp directory is intended to be world-writable, there is a risk of resource exhaustion and exploitation if it is not bound to a separate partition."
            level: 1
            scored: true
            audit_command: |
              findmnt --kernel /tmp
            expected_output: |
              /tmp is listed as a separate mount point
            remediation: |
              systemctl unmask tmp.mount
              systemctl enable tmp.mount

          - number: "1.1.5"
            title: "Ensure nodev option set on /tmp partition"
            description: "The nodev mount option specifies that the filesystem cannot contain special devices."
            rationale: "Since the /tmp filesystem is not intended to support devices, set this option to ensure that users cannot create block or character special devices in /tmp."
            level: 1
            scored: true
            audit_command: |
              findmnt --kernel /tmp | grep nodev
            expected_output: |
              nodev option is present
            remediation: |
              mount -o remount,nodev /tmp

          - number: "1.1.6"
            title: "Ensure noexec option set on /tmp partition"
            description: "The noexec mount option specifies that the filesystem cannot contain executable binaries."
            rationale: "Since the /tmp filesystem is only intended for temporary file storage, set this option to ensure that users cannot run executable binaries from /tmp."
            level: 1
            scored: true
            audit_command: |
              findmnt --kernel /tmp | grep noexec
            expected_output: |
              noexec option is present
            remediation: |
              mount -o remount,noexec /tmp

      - number: "1.2"
        title: "Configure Software Updates"
        checks:
          - number: "1.2.1"
            title: "Ensure GPG keys are configured"
            description: "Most package managers implement GPG key signing to verify package integrity during installation."
            rationale: "It is important to ensure that updates are obtained from a valid source to protect against spoofing."
            level: 1
            scored: false
            audit_command: |
              rpm -q gpg-pubkey --qf '%{name}-%{version}-%{release} --> %{summary}\n'
            expected_output: |
              gpg-pubkey
            remediation: |
              rpm --import /etc/pki/rpm-gpg/RPM-GPG-KEY-redhat-release

          - number: "1.2.2"
            title: "Ensure Red Hat Subscription Manager connection is configured"
            description: "Systems need to be registered with the Red Hat Subscription Manager to receive the latest patches and updates."
            rationale: "If a system is not registered with Red Hat Subscription Manager, it will not receive critical security patches."
            level: 1
            scored: false
            audit_command: |
              subscription-manager identity
            expected_output: |
              system identity (registered)
            remediation: |
              subscription-manager register

          - number: "1.2.3"
            title: "Ensure package manager repositories are configured"
            description: "Systems need to have package manager repositories configured to ensure they receive the latest patches and updates."
            rationale: "If a system's package repositories are misconfigured, important patches may not be installed."
            level: 1
            scored: false
            audit_command: |
              dnf repolist
            expected_output: |
              repo id
            remediation: |
              Configure your package manager repositories according to site policy.

      - number: "1.3"
        title: "Filesystem Integrity Checking"
        checks:
          - number: "1.3.1"
            title: "Ensure AIDE is installed"
            description: "AIDE takes a snapshot of filesystem state including modification times, permissions, and file hashes which can then be used to compare against the current state of the filesystem to detect modifications to the system."
            rationale: "By monitoring the filesystem state compromised files can be detected to prevent or limit the exposure of accidental or malicious misconfigurations or modified binaries."
            level: 1
            scored: true
            audit_command: |
              rpm -q aide
            expected_output: |
              aide-
            remediation: |
              dnf install aide
              aide --init
              mv /var/lib/aide/aide.db.new.gz /var/lib/aide/aide.db.gz

          - number: "1.3.2"
            title: "Ensure filesystem integrity is regularly checked"
            description: "Periodic checking of the filesystem integrity is needed to detect changes to the filesystem."
            rationale: "Periodic file checking allows the system administrator to determine on a regular basis if critical files have been changed in an unauthorized fashion."
            level: 1
            scored: true
            audit_command: |
              systemctl is-enabled aidecheck.timer 2>/dev/null || grep -r aide /etc/crontab /etc/cron.*
            expected_output: |
              aide check is scheduled
            remediation: |
              echo "0 5 * * * /usr/sbin/aide --check" >> /etc/crontab

      - number: "1.4"
        title: "Secure Boot Settings"
        checks:
          - number: "1.4.1"
            title: "Ensure bootloader password is set"
            description: "Setting the boot loader password will require that anyone rebooting the system must enter a password before being able to set command line boot parameters."
            rationale: "Requiring a boot password upon execution of the boot loader will prevent an unauthorized user from entering boot parameters or changing the boot partition."
            level: 1
            scored: true
            audit_command: |
              grep "^GRUB2_PASSWORD" /boot/grub2/user.cfg 2>/dev/null
            expected_output: |
              GRUB2_PASSWORD=
            remediation: |
              grub2-setpassword
              grub2-mkconfig -o /boot/grub2/grub.cfg

          - number: "1.4.2"
            title: "Ensure permissions on bootloader config are configured"
            description: "The grub configuration file contains information on boot settings and passwords for unlocking boot options."
            rationale: "Setting the permissions to read and write for root only prevents non-root users from seeing the boot parameters or changing them."
            level: 1
            scored: true
            audit_command: |
              stat -c "%a %u %g" /boot/grub2/grub.cfg
            expected_output: |
              600 0 0
            remediation: |
              chown root:root /boot/grub2/grub.cfg
              chmod 600 /boot/grub2/grub.cfg

      - number: "1.5"
        title: "Mandatory Access Control"
        checks:
          - number: "1.5.1"
            title: "Ensure SELinux is installed"
            description: "SELinux provides Mandatory Access Controls (MAC) to Red Hat Enterprise Linux 8."
            rationale: "Without a Mandatory Access Control system installed only the default Discretionary Access Control system will be available."
            level: 1
            scored: true
            audit_command: |
              rpm -q libselinux
            expected_output: |
              libselinux-
            remediation: |
              dnf install libselinux

          - number: "1.5.2"
            title: "Ensure SELinux is not disabled in bootloader configuration"
            description: "Configure SELinux to be enabled at boot time and verify that it has not been overwritten by the grub boot parameters."
            rationale: "SELinux must be enabled at boot time to ensure that the controls it provides are not overridden."
            level: 1
            scored: true
            audit_command: |
              grubby --info=ALL 2>/dev/null | grep -E 'selinux=0|enforcing=0' || echo "PASS"
            expected_output: |
              PASS
            remediation: |
              grubby --update-kernel ALL --remove-args "selinux=0 enforcing=0"

          - number: "1.5.3"
            title: "Ensure SELinux policy is configured"
            description: "Configure SELinux to meet or exceed the default targeted policy."
            rationale: "Security configuration requirements state that at a minimum, the default targeted policy is used."
            level: 1
            scored: true
            audit_command: |
              sestatus | grep "Loaded policy name"
            expected_output: |
              Loaded policy name:             targeted
            remediation: |
              sed -i 's/^SELINUXTYPE=.*/SELINUXTYPE=targeted/' /etc/selinux/config

          - number: "1.5.4"
            title: "Ensure SELinux mode is enforcing"
            description: "Set SELinux to enable when the system is booted and verify it is in enforcing mode."
            rationale: "SELinux must be enabled and in enforcing mode at boot time to ensure full protection."
            level: 2
            scored: true
            audit_command: |
              getenforce
            expected_output: |
              Enforcing
            remediation: |
              sed -i 's/^SELINUX=.*/SELINUX=enforcing/' /etc/selinux/config
              setenforce 1

      - number: "1.6"
        title: "Crypto Policies"
        checks:
          - number: "1.6.1"
            title: "Ensure system-wide crypto policy is not legacy"
            description: "The system-wide crypto-policies followed by the crypto-core libraries when building TLS, IPSec, DNSSEC, Kerberos, etc."
            rationale: "If the Legacy policy is selected, it includes support for TLS 1.0, TLS 1.1, and SSH2 protocols or later. These are considered to have known weaknesses."
            level: 1
            scored: true
            audit_command: |
              update-crypto-policies --show
            expected_output: |
              DEFAULT (or FUTURE or FIPS)
            remediation: |
              update-crypto-policies --set DEFAULT
              update-crypto-policies

          - number: "1.6.2"
            title: "Ensure system-wide crypto policy disables SHA1 hash and CBC mode ciphers"
            description: "The system-wide crypto policies can be customized using policy modules to disable specific hash algorithms and cipher modes."
            rationale: "SHA1 hash algorithm and CBC mode ciphers are considered weak and vulnerable to attacks."
            level: 2
            scored: true
            audit_command: |
              update-crypto-policies --show; grep -i sha1 /etc/crypto-policies/state/CURRENT.pol
            expected_output: |
              No SHA1 in active policy
            remediation: |
              update-crypto-policies --set DEFAULT:NO-SHA1

  # ===========================================================================
  # Section 2: Services
  # ===========================================================================
  - number: "2"
    title: "Services"
    children:
      - number: "2.1"
        title: "Special Purpose Services"
        checks:
          - number: "2.1.1"
            title: "Ensure time synchronization is in use"
            description: "System time should be synchronized between all systems in an environment using chrony or NTP."
            rationale: "Time synchronization is important to support time sensitive security mechanisms and to ensure log management is consistent."
            level: 1
            scored: true
            audit_command: |
              rpm -q chrony
            expected_output: |
              chrony-
            remediation: |
              dnf install chrony
              systemctl enable chronyd
              systemctl start chronyd

          - number: "2.1.2"
            title: "Ensure chrony is configured"
            description: "chrony is a daemon which implements the Network Time Protocol (NTP) and is designed to synchronize system clocks across a variety of systems."
            rationale: "If chrony is in use on the system proper configuration is vital to ensuring time synchronization is working properly."
            level: 1
            scored: true
            audit_command: |
              grep -E "^(server|pool)" /etc/chrony.conf
            expected_output: |
              server or pool entries present
            remediation: |
              Edit /etc/chrony.conf and add NTP server entries.
              systemctl restart chronyd

          - number: "2.1.3"
            title: "Ensure X11 Server components are not installed"
            description: "The X Window System provides a Graphical User Interface (GUI) where users can have multiple windows in which to run programs."
            rationale: "Unless your organization specifically requires graphical login access via X Windows, remove it to reduce the potential attack surface."
            level: 1
            scored: true
            audit_command: |
              rpm -qa xorg-x11-server*
            expected_output: ""
            remediation: |
              dnf remove xorg-x11-server*

          - number: "2.1.4"
            title: "Ensure Avahi Server is not installed"
            description: "Avahi is a free zeroconf implementation, including a system for multicast DNS/DNS-SD service discovery."
            rationale: "Since servers are not normally browsed by other computers, this service is not needed on a server."
            level: 1
            scored: true
            audit_command: |
              rpm -q avahi
            expected_output: |
              package avahi is not installed
            remediation: |
              systemctl stop avahi-daemon.socket avahi-daemon.service
              dnf remove avahi

          - number: "2.1.5"
            title: "Ensure CUPS is not installed"
            description: "The Common Unix Print System (CUPS) provides the ability to print to both local and network printers."
            rationale: "If the system does not need to print jobs or accept print jobs from other systems, it is recommended that CUPS be removed."
            level: 1
            scored: true
            audit_command: |
              rpm -q cups
            expected_output: |
              package cups is not installed
            remediation: |
              dnf remove cups

          - number: "2.1.6"
            title: "Ensure DHCP Server is not installed"
            description: "The Dynamic Host Configuration Protocol (DHCP) is a service that allows machines to be automatically assigned IP addresses."
            rationale: "Unless a system is specifically set up to act as a DHCP server, it is recommended to remove the package."
            level: 1
            scored: true
            audit_command: |
              rpm -q dhcp-server
            expected_output: |
              package dhcp-server is not installed
            remediation: |
              dnf remove dhcp-server

          - number: "2.1.7"
            title: "Ensure DNS Server is not installed"
            description: "The Domain Name System (DNS) is a hierarchical naming system that maps names to IP addresses."
            rationale: "Unless a system is specifically designated to act as a DNS server, it is recommended to remove the package."
            level: 1
            scored: true
            audit_command: |
              rpm -q bind
            expected_output: |
              package bind is not installed
            remediation: |
              dnf remove bind

          - number: "2.1.8"
            title: "Ensure FTP Server is not installed"
            description: "FTP allows users to transfer files between systems over a network. FTP does not protect the confidentiality of data or authentication credentials."
            rationale: "FTP does not protect the confidentiality of data or authentication credentials. Use SFTP or SCP instead."
            level: 1
            scored: true
            audit_command: |
              rpm -q vsftpd
            expected_output: |
              package vsftpd is not installed
            remediation: |
              dnf remove vsftpd

          - number: "2.1.9"
            title: "Ensure HTTP server is not installed"
            description: "HTTP or web servers provide the ability to host web site content."
            rationale: "Unless there is a need to run the system as a web server, it is recommended that the package be removed."
            level: 1
            scored: true
            audit_command: |
              rpm -q httpd nginx
            expected_output: |
              package httpd is not installed
              package nginx is not installed
            remediation: |
              dnf remove httpd nginx

          - number: "2.1.10"
            title: "Ensure IMAP and POP3 server is not installed"
            description: "Dovecot provides IMAP and POP3 services for email retrieval."
            rationale: "Unless POP3 or IMAP servers are to be provided by this system, the package should be removed."
            level: 1
            scored: true
            audit_command: |
              rpm -q dovecot cyrus-imapd
            expected_output: |
              package dovecot is not installed
              package cyrus-imapd is not installed
            remediation: |
              dnf remove dovecot cyrus-imapd

          - number: "2.1.11"
            title: "Ensure Samba is not installed"
            description: "The Samba daemon allows system administrators to configure their Linux systems to share file systems and directories with Windows desktops."
            rationale: "If there is no need to mount directories and file systems from Windows servers, then Samba should be removed."
            level: 1
            scored: true
            audit_command: |
              rpm -q samba
            expected_output: |
              package samba is not installed
            remediation: |
              dnf remove samba

          - number: "2.1.12"
            title: "Ensure NFS is not installed"
            description: "The Network File System (NFS) is one of the first and most widely distributed file systems in the UNIX environment."
            rationale: "If the system does not export NFS shares, the nfs-utils package can be removed to reduce the attack surface."
            level: 1
            scored: true
            audit_command: |
              rpm -q nfs-utils
            expected_output: |
              package nfs-utils is not installed
            remediation: |
              dnf remove nfs-utils

  # ===========================================================================
  # Section 3: Network Configuration
  # ===========================================================================
  - number: "3"
    title: "Network Configuration"
    children:
      - number: "3.1"
        title: "Disable unused network protocols and devices"
        checks:
          - number: "3.1.1"
            title: "Ensure system is checked to determine if IPv6 is enabled"
            description: "Internet Protocol version 6 (IPv6) is the most recent version of the Internet Protocol."
            rationale: "If IPv6 is not required on the system, it is recommended to disable it to reduce the attack surface."
            level: 1
            scored: false
            audit_command: |
              sysctl net.ipv6.conf.all.disable_ipv6; sysctl net.ipv6.conf.default.disable_ipv6
            expected_output: |
              Verify IPv6 status
            remediation: |
              echo "net.ipv6.conf.all.disable_ipv6 = 1" >> /etc/sysctl.d/60-disable-ipv6.conf
              echo "net.ipv6.conf.default.disable_ipv6 = 1" >> /etc/sysctl.d/60-disable-ipv6.conf
              sysctl -p /etc/sysctl.d/60-disable-ipv6.conf

          - number: "3.1.2"
            title: "Ensure wireless interfaces are disabled"
            description: "Wireless networking is used when wired networks are unavailable."
            rationale: "If wireless is not to be used, wireless devices can be disabled to reduce the potential attack surface."
            level: 1
            scored: true
            audit_command: |
              nmcli radio all
            expected_output: |
              WIFI-HW  WIFI  WWAN-HW  WWAN
              enabled  disabled  enabled  disabled
            remediation: |
              nmcli radio wifi off

      - number: "3.2"
        title: "Network Parameters"
        checks:
          - number: "3.2.1"
            title: "Ensure IP forwarding is disabled"
            description: "The net.ipv4.ip_forward flag is used to tell the system that it can forward packets between interfaces."
            rationale: "Setting the flag to 0 ensures that a system with multiple interfaces does not forward packets, which is only appropriate for routers."
            level: 1
            scored: true
            audit_command: |
              sysctl net.ipv4.ip_forward; sysctl net.ipv6.conf.all.forwarding
            expected_output: |
              net.ipv4.ip_forward = 0
              net.ipv6.conf.all.forwarding = 0
            remediation: |
              echo "net.ipv4.ip_forward = 0" >> /etc/sysctl.d/60-netipv4_sysctl.conf
              echo "net.ipv6.conf.all.forwarding = 0" >> /etc/sysctl.d/60-netipv6_sysctl.conf
              sysctl -w net.ipv4.ip_forward=0
              sysctl -w net.ipv6.conf.all.forwarding=0

          - number: "3.2.2"
            title: "Ensure packet redirect sending is disabled"
            description: "ICMP Redirects are used to send routing information to other hosts."
            rationale: "An attacker could use a compromised host to send invalid ICMP redirects to other router devices."
            level: 1
            scored: true
            audit_command: |
              sysctl net.ipv4.conf.all.send_redirects; sysctl net.ipv4.conf.default.send_redirects
            expected_output: |
              net.ipv4.conf.all.send_redirects = 0
              net.ipv4.conf.default.send_redirects = 0
            remediation: |
              echo "net.ipv4.conf.all.send_redirects = 0" >> /etc/sysctl.d/60-netipv4_sysctl.conf
              echo "net.ipv4.conf.default.send_redirects = 0" >> /etc/sysctl.d/60-netipv4_sysctl.conf

      - number: "3.3"
        title: "Network Parameters (Host and Router)"
        checks:
          - number: "3.3.1"
            title: "Ensure source routed packets are not accepted"
            description: "In networking, source routing allows a sender to partially or fully specify the route packets take through a network."
            rationale: "Setting net.ipv4.conf.all.accept_source_route to 0 disables the system from accepting source routed packets."
            level: 1
            scored: true
            audit_command: |
              sysctl net.ipv4.conf.all.accept_source_route; sysctl net.ipv4.conf.default.accept_source_route
            expected_output: |
              net.ipv4.conf.all.accept_source_route = 0
              net.ipv4.conf.default.accept_source_route = 0
            remediation: |
              echo "net.ipv4.conf.all.accept_source_route = 0" >> /etc/sysctl.d/60-netipv4_sysctl.conf
              echo "net.ipv4.conf.default.accept_source_route = 0" >> /etc/sysctl.d/60-netipv4_sysctl.conf

          - number: "3.3.2"
            title: "Ensure ICMP redirects are not accepted"
            description: "ICMP redirect messages are packets that convey routing information and tell your host to send packets via an alternate path."
            rationale: "Attackers could use bogus ICMP redirect messages to maliciously alter the system routing tables."
            level: 1
            scored: true
            audit_command: |
              sysctl net.ipv4.conf.all.accept_redirects; sysctl net.ipv4.conf.default.accept_redirects
            expected_output: |
              net.ipv4.conf.all.accept_redirects = 0
              net.ipv4.conf.default.accept_redirects = 0
            remediation: |
              echo "net.ipv4.conf.all.accept_redirects = 0" >> /etc/sysctl.d/60-netipv4_sysctl.conf
              echo "net.ipv4.conf.default.accept_redirects = 0" >> /etc/sysctl.d/60-netipv4_sysctl.conf

          - number: "3.3.3"
            title: "Ensure suspicious packets are logged"
            description: "When enabled, this feature logs packets with un-routable source addresses to the kernel log."
            rationale: "Enabling this feature and logging these packets allows an administrator to investigate spoofed packets."
            level: 1
            scored: true
            audit_command: |
              sysctl net.ipv4.conf.all.log_martians; sysctl net.ipv4.conf.default.log_martians
            expected_output: |
              net.ipv4.conf.all.log_martians = 1
              net.ipv4.conf.default.log_martians = 1
            remediation: |
              echo "net.ipv4.conf.all.log_martians = 1" >> /etc/sysctl.d/60-netipv4_sysctl.conf
              echo "net.ipv4.conf.default.log_martians = 1" >> /etc/sysctl.d/60-netipv4_sysctl.conf

      - number: "3.4"
        title: "Firewall Configuration"
        checks:
          - number: "3.4.1"
            title: "Ensure firewalld is installed"
            description: "firewalld is a firewall management tool with support for network/firewall zones."
            rationale: "A firewall utility is required to configure the Linux kernel's netfilter framework."
            level: 1
            scored: true
            audit_command: |
              rpm -q firewalld
            expected_output: |
              firewalld-
            remediation: |
              dnf install firewalld

          - number: "3.4.2"
            title: "Ensure firewalld service is enabled and running"
            description: "Ensure that the firewalld service is enabled to protect the system from unauthorized network access."
            rationale: "firewalld needs to be enabled and running to filter network traffic and protect the system."
            level: 1
            scored: true
            audit_command: |
              systemctl is-enabled firewalld; systemctl is-active firewalld
            expected_output: |
              enabled
              active
            remediation: |
              systemctl enable firewalld
              systemctl start firewalld

          - number: "3.4.3"
            title: "Ensure nftables is not installed or stopped and masked"
            description: "nftables is a subsystem of the Linux kernel providing filtering and classification of network packets."
            rationale: "Running both firewalld and nftables may lead to conflict. Ensure only one firewall utility is in use."
            level: 1
            scored: true
            audit_command: |
              rpm -q nftables; systemctl is-active nftables
            expected_output: |
              package nftables is not installed (or inactive)
            remediation: |
              systemctl stop nftables
              systemctl mask nftables

  # ===========================================================================
  # Section 4: Logging and Auditing
  # ===========================================================================
  - number: "4"
    title: "Logging and Auditing"
    children:
      - number: "4.1"
        title: "Configure System Accounting (auditd)"
        checks:
          - number: "4.1.1"
            title: "Ensure auditd is installed"
            description: "auditd is the userspace component to the Linux Auditing System."
            rationale: "The capturing of system events provides system administrators with information to allow them to determine if unauthorized access to their system is occurring."
            level: 2
            scored: true
            audit_command: |
              rpm -q audit audit-libs
            expected_output: |
              audit-
              audit-libs-
            remediation: |
              dnf install audit audit-libs

          - number: "4.1.2"
            title: "Ensure auditd service is enabled and active"
            description: "Enable and start the auditd daemon to record system events."
            rationale: "The capturing of system events provides system administrators with evidence of unauthorized activity."
            level: 2
            scored: true
            audit_command: |
              systemctl is-enabled auditd; systemctl is-active auditd
            expected_output: |
              enabled
              active
            remediation: |
              systemctl enable auditd

          - number: "4.1.3"
            title: "Ensure auditing for processes that start prior to auditd is enabled"
            description: "Configure grub so that processes that are capable of being audited can be audited even if they start up prior to auditd startup."
            rationale: "Audit events need to be captured on processes that start up prior to auditd."
            level: 2
            scored: true
            audit_command: |
              grubby --info=ALL | grep -Po '\baudit=1\b'
            expected_output: |
              audit=1
            remediation: |
              grubby --update-kernel ALL --args 'audit=1'

          - number: "4.1.4"
            title: "Ensure events that modify date and time information are collected"
            description: "Capture events where the system date or time has been modified."
            rationale: "Unexpected changes in system date or time could be a sign of malicious activity."
            level: 2
            scored: true
            audit_command: |
              auditctl -l | grep time-change
            expected_output: |
              -a always,exit -F arch=b64 -S adjtimex,settimeofday -k time-change
            remediation: |
              echo "-a always,exit -F arch=b64 -S adjtimex -S settimeofday -k time-change" >> /etc/audit/rules.d/50-time-change.rules
              echo "-w /etc/localtime -p wa -k time-change" >> /etc/audit/rules.d/50-time-change.rules

      - number: "4.2"
        title: "Configure Logging"
        checks:
          - number: "4.2.1"
            title: "Ensure rsyslog is installed"
            description: "The rsyslog software is the recommended replacement to the original syslogd daemon."
            rationale: "The security enhancements and additional logging capabilities of rsyslog warrant its use over syslogd."
            level: 1
            scored: true
            audit_command: |
              rpm -q rsyslog
            expected_output: |
              rsyslog-
            remediation: |
              dnf install rsyslog

          - number: "4.2.2"
            title: "Ensure rsyslog service is enabled"
            description: "Enable the rsyslog service to ensure system logging is active."
            rationale: "If the rsyslog service is not activated the system may default to the syslogd service or lack logging entirely."
            level: 1
            scored: true
            audit_command: |
              systemctl is-enabled rsyslog
            expected_output: |
              enabled
            remediation: |
              systemctl enable rsyslog

          - number: "4.2.3"
            title: "Ensure journald is configured to send logs to rsyslog"
            description: "Data from journald may be stored in volatile memory or persisted locally on the server."
            rationale: "Storing log data on a remote host protects log integrity from local attacks and provides a centralized view."
            level: 1
            scored: true
            audit_command: |
              grep -E '^\s*ForwardToSyslog' /etc/systemd/journald.conf
            expected_output: |
              ForwardToSyslog=yes
            remediation: |
              echo "ForwardToSyslog=yes" >> /etc/systemd/journald.conf
              systemctl restart systemd-journald

  # ===========================================================================
  # Section 5: Access, Authentication and Authorization
  # ===========================================================================
  - number: "5"
    title: "Access, Authentication and Authorization"
    children:
      - number: "5.1"
        title: "Configure time-based job schedulers"
        checks:
          - number: "5.1.1"
            title: "Ensure cron daemon is enabled and running"
            description: "The cron daemon is used to execute batch jobs on the system."
            rationale: "While there may not be user jobs that need to be run, the system does have maintenance jobs that may include security monitoring."
            level: 1
            scored: true
            audit_command: |
              systemctl is-enabled crond; systemctl is-active crond
            expected_output: |
              enabled
              active
            remediation: |
              systemctl enable crond

          - number: "5.1.2"
            title: "Ensure permissions on /etc/crontab are configured"
            description: "The /etc/crontab file is used by cron to control its own jobs."
            rationale: "This file contains information on what system jobs are run by cron. Write access could provide unprivileged users with the ability to elevate their privileges."
            level: 1
            scored: true
            audit_command: |
              stat -c "%a %U %G" /etc/crontab
            expected_output: |
              600 root root
            remediation: |
              chown root:root /etc/crontab
              chmod 600 /etc/crontab

      - number: "5.2"
        title: "Configure SSH Server"
        checks:
          - number: "5.2.1"
            title: "Ensure permissions on /etc/ssh/sshd_config are configured"
            description: "The /etc/ssh/sshd_config file contains configuration specifications for sshd."
            rationale: "The /etc/ssh/sshd_config file needs to be protected from unauthorized changes by non-privileged users."
            level: 1
            scored: true
            audit_command: |
              stat -c "%a %U %G" /etc/ssh/sshd_config
            expected_output: |
              600 root root
            remediation: |
              chown root:root /etc/ssh/sshd_config
              chmod 600 /etc/ssh/sshd_config

          - number: "5.2.2"
            title: "Ensure SSH access is limited"
            description: "There are several options available to limit which users and groups are allowed to access the system via SSH."
            rationale: "Restricting which users can remotely access the system via SSH will help ensure that only authorized users access the system."
            level: 1
            scored: true
            audit_command: |
              sshd -T | grep -Ei '^\s*(allow|deny)(users|groups)\s'
            expected_output: |
              allowusers or allowgroups is configured
            remediation: |
              Edit /etc/ssh/sshd_config and set AllowUsers or AllowGroups.
              systemctl restart sshd

          - number: "5.2.3"
            title: "Ensure SSH LogLevel is appropriate"
            description: "The INFO or VERBOSE parameter specifies that login and logout activity will be logged."
            rationale: "SSH provides several logging levels. INFO level is the basic level that records login activity."
            level: 1
            scored: true
            audit_command: |
              sshd -T | grep loglevel
            expected_output: |
              loglevel INFO (or VERBOSE)
            remediation: |
              sed -i 's/^#*LogLevel.*/LogLevel INFO/' /etc/ssh/sshd_config
              systemctl restart sshd

          - number: "5.2.4"
            title: "Ensure SSH root login is disabled"
            description: "The PermitRootLogin parameter specifies if the root user can log in using ssh."
            rationale: "Disallowing root logins over SSH requires system admins to authenticate using their own individual account, then escalating to root via sudo."
            level: 1
            scored: true
            audit_command: |
              sshd -T | grep permitrootlogin
            expected_output: |
              permitrootlogin no
            remediation: |
              sed -i 's/^#*PermitRootLogin.*/PermitRootLogin no/' /etc/ssh/sshd_config
              systemctl restart sshd

          - number: "5.2.5"
            title: "Ensure SSH PermitEmptyPasswords is disabled"
            description: "The PermitEmptyPasswords parameter specifies if the SSH server allows login to accounts with empty password strings."
            rationale: "Disallowing remote shell access to accounts that have an empty password reduces the probability of unauthorized access."
            level: 1
            scored: true
            audit_command: |
              sshd -T | grep permitemptypasswords
            expected_output: |
              permitemptypasswords no
            remediation: |
              sed -i 's/^#*PermitEmptyPasswords.*/PermitEmptyPasswords no/' /etc/ssh/sshd_config
              systemctl restart sshd

          - number: "5.2.6"
            title: "Ensure SSH MaxAuthTries is set to 4 or less"
            description: "The MaxAuthTries parameter specifies the maximum number of authentication attempts permitted per connection."
            rationale: "Setting the MaxAuthTries parameter to a low number will minimize the risk of successful brute force attacks."
            level: 1
            scored: true
            audit_command: |
              sshd -T | grep maxauthtries
            expected_output: |
              maxauthtries 4
            remediation: |
              sed -i 's/^#*MaxAuthTries.*/MaxAuthTries 4/' /etc/ssh/sshd_config
              systemctl restart sshd

          - number: "5.2.7"
            title: "Ensure SSH MaxSessions is limited"
            description: "The MaxSessions parameter specifies the maximum number of open sessions permitted from a given connection."
            rationale: "To protect a system from denial of service due to a large number of concurrent sessions, use the rate limiting function of MaxSessions."
            level: 1
            scored: true
            audit_command: |
              sshd -T | grep -i maxsessions
            expected_output: |
              maxsessions 10
            remediation: |
              sed -i 's/^#*MaxSessions.*/MaxSessions 10/' /etc/ssh/sshd_config
              systemctl restart sshd

      - number: "5.3"
        title: "Configure privilege escalation"
        checks:
          - number: "5.3.1"
            title: "Ensure sudo is installed"
            description: "sudo allows a permitted user to execute a command as the superuser or another user."
            rationale: "sudo supports a plugin architecture for security policies and input/output logging. It is the standard method for privilege escalation."
            level: 1
            scored: true
            audit_command: |
              rpm -q sudo
            expected_output: |
              sudo-
            remediation: |
              dnf install sudo

          - number: "5.3.2"
            title: "Ensure sudo commands use pty"
            description: "sudo can be configured to run only from a pseudo terminal (pseudo-pty)."
            rationale: "Attackers can run a malicious program using sudo which would again fork a background process that remains even when the main program has finished executing."
            level: 1
            scored: true
            audit_command: |
              grep -Ei '^\s*Defaults\s+(\[^#]+,\s*)?use_pty' /etc/sudoers /etc/sudoers.d/*
            expected_output: |
              Defaults use_pty
            remediation: |
              echo "Defaults use_pty" >> /etc/sudoers.d/use_pty

          - number: "5.3.3"
            title: "Ensure sudo log file exists"
            description: "sudo can use a custom log file."
            rationale: "A sudo log file simplifies auditing of sudo commands."
            level: 1
            scored: true
            audit_command: |
              grep -Ei '^\s*Defaults\s+logfile=' /etc/sudoers /etc/sudoers.d/*
            expected_output: |
              Defaults logfile="/var/log/sudo.log"
            remediation: |
              echo 'Defaults logfile="/var/log/sudo.log"' >> /etc/sudoers.d/sudo_log

      - number: "5.4"
        title: "Configure PAM"
        checks:
          - number: "5.4.1"
            title: "Ensure password creation requirements are configured"
            description: "The pam_pwquality.so module checks the strength of passwords."
            rationale: "Strong passwords protect systems from being hacked through brute force methods."
            level: 1
            scored: true
            audit_command: |
              grep pam_pwquality.so /etc/pam.d/password-auth /etc/pam.d/system-auth
            expected_output: |
              password requisite pam_pwquality.so try_first_pass retry=3
            remediation: |
              Edit /etc/security/pwquality.conf:
              minlen = 14
              minclass = 4

          - number: "5.4.2"
            title: "Ensure lockout for failed password attempts is configured"
            description: "Lock out users after a configurable number of unsuccessful consecutive login attempts."
            rationale: "Locking out user IDs after unsuccessful consecutive attempts mitigates brute force password attacks."
            level: 1
            scored: true
            audit_command: |
              grep -E 'pam_faillock' /etc/pam.d/password-auth /etc/pam.d/system-auth
            expected_output: |
              auth required pam_faillock.so
            remediation: |
              Edit /etc/security/faillock.conf:
              deny = 5
              unlock_time = 900

          - number: "5.4.3"
            title: "Ensure password reuse is limited"
            description: "The /etc/security/opasswd file stores the users old passwords and can be checked to ensure that users are not recycling recent passwords."
            rationale: "Forcing users not to reuse their past passwords reduces the risk of password reuse attacks."
            level: 1
            scored: true
            audit_command: |
              grep -E 'remember=' /etc/pam.d/system-auth
            expected_output: |
              password sufficient pam_unix.so ... remember=5
            remediation: |
              Edit /etc/pam.d/system-auth and /etc/pam.d/password-auth:
              password sufficient pam_unix.so remember=5

      - number: "5.5"
        title: "User Accounts and Environment"
        checks:
          - number: "5.5.1"
            title: "Ensure password expiration is 365 days or less"
            description: "The PASS_MAX_DAYS parameter in /etc/login.defs allows an administrator to force passwords to expire."
            rationale: "The window of opportunity for an attacker to leverage compromised credentials is limited by the age of the password."
            level: 1
            scored: true
            audit_command: |
              grep PASS_MAX_DAYS /etc/login.defs
            expected_output: |
              PASS_MAX_DAYS 365
            remediation: |
              sed -i 's/^PASS_MAX_DAYS.*/PASS_MAX_DAYS 365/' /etc/login.defs

          - number: "5.5.2"
            title: "Ensure minimum days between password changes is configured"
            description: "The PASS_MIN_DAYS parameter in /etc/login.defs allows an administrator to prevent users from changing their password too frequently."
            rationale: "By restricting the frequency of password changes, an administrator can prevent users from repeatedly changing their password to defeat reuse controls."
            level: 1
            scored: true
            audit_command: |
              grep PASS_MIN_DAYS /etc/login.defs
            expected_output: |
              PASS_MIN_DAYS 1
            remediation: |
              sed -i 's/^PASS_MIN_DAYS.*/PASS_MIN_DAYS 1/' /etc/login.defs

          - number: "5.5.3"
            title: "Ensure password expiration warning days is 7 or more"
            description: "The PASS_WARN_AGE parameter in /etc/login.defs allows an administrator to notify users that their password will expire."
            rationale: "Providing an advance warning that a password will be expiring gives users time to think of a secure password."
            level: 1
            scored: true
            audit_command: |
              grep PASS_WARN_AGE /etc/login.defs
            expected_output: |
              PASS_WARN_AGE 7
            remediation: |
              sed -i 's/^PASS_WARN_AGE.*/PASS_WARN_AGE 7/' /etc/login.defs

          - number: "5.5.4"
            title: "Ensure inactive password lock is 30 days or less"
            description: "User accounts that have been inactive for an extended period of time can be automatically disabled."
            rationale: "Inactive accounts pose a threat to system security since the users are not logging in to notice anomalies."
            level: 1
            scored: true
            audit_command: |
              useradd -D | grep INACTIVE
            expected_output: |
              INACTIVE=30
            remediation: |
              useradd -D -f 30

  # ===========================================================================
  # Section 6: System Maintenance
  # ===========================================================================
  - number: "6"
    title: "System Maintenance"
    children:
      - number: "6.1"
        title: "System File Permissions"
        checks:
          - number: "6.1.1"
            title: "Ensure permissions on /etc/passwd are configured"
            description: "The /etc/passwd file contains user account information that is used by many system utilities."
            rationale: "It is critical to ensure that the /etc/passwd file is protected from unauthorized write access."
            level: 1
            scored: true
            audit_command: |
              stat -c "%a %U %G" /etc/passwd
            expected_output: |
              644 root root
            remediation: |
              chown root:root /etc/passwd
              chmod 644 /etc/passwd

          - number: "6.1.2"
            title: "Ensure permissions on /etc/shadow are configured"
            description: "The /etc/shadow file is used to store the information about user accounts that is critical to the security of those accounts."
            rationale: "If attackers can gain read access to the /etc/shadow file, they can easily run a password cracking program."
            level: 1
            scored: true
            audit_command: |
              stat -c "%a %U %G" /etc/shadow
            expected_output: |
              0 root root
            remediation: |
              chown root:root /etc/shadow
              chmod 0000 /etc/shadow

          - number: "6.1.3"
            title: "Ensure permissions on /etc/group are configured"
            description: "The /etc/group file contains a list of all the valid groups defined in the system."
            rationale: "The /etc/group file needs to be protected from unauthorized changes by non-privileged users."
            level: 1
            scored: true
            audit_command: |
              stat -c "%a %U %G" /etc/group
            expected_output: |
              644 root root
            remediation: |
              chown root:root /etc/group
              chmod 644 /etc/group

          - number: "6.1.4"
            title: "Ensure permissions on /etc/gshadow are configured"
            description: "The /etc/gshadow file is used to store the information about groups that is critical to the security of those groups."
            rationale: "If attackers can gain read access to the /etc/gshadow file, they can run a password cracking program against the hashed passwords."
            level: 1
            scored: true
            audit_command: |
              stat -c "%a %U %G" /etc/gshadow
            expected_output: |
              0 root root
            remediation: |
              chown root:root /etc/gshadow
              chmod 0000 /etc/gshadow

      - number: "6.2"
        title: "User and Group Settings"
        checks:
          - number: "6.2.1"
            title: "Ensure accounts in /etc/passwd use shadowed passwords"
            description: "Local accounts can use shadowed passwords. With shadowed passwords, the passwords are saved in shadow password file /etc/shadow."
            rationale: "The /etc/passwd file contains user account information. The password field should be set to 'x' indicating shadowed passwords."
            level: 1
            scored: true
            audit_command: |
              awk -F: '($2 != "x" ) { print $1 }' /etc/passwd
            expected_output: ""
            remediation: |
              Run pwconv to convert all accounts to use shadowed passwords.

          - number: "6.2.2"
            title: "Ensure no duplicate UIDs exist"
            description: "Although the useradd program will not let you create a duplicate UID, it is possible for an administrator to manually edit /etc/passwd."
            rationale: "Users must be assigned unique UIDs for accountability and to ensure appropriate access protections."
            level: 1
            scored: true
            audit_command: |
              cut -f3 -d":" /etc/passwd | sort -n | uniq -c | awk '$1 > 1 {print}'
            expected_output: ""
            remediation: |
              Based on the results, establish unique UIDs and review all files owned by the shared UIDs.

          - number: "6.2.3"
            title: "Ensure root is the only UID 0 account"
            description: "Any account with UID 0 has superuser privileges on the system."
            rationale: "This access must be limited to only the default root account."
            level: 1
            scored: true
            audit_command: |
              awk -F: '($3 == 0) { print $1 }' /etc/passwd
            expected_output: |
              root
            remediation: |
              Remove any users other than root with UID 0 or assign them a new UID.
