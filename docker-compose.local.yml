# Local testing overlay: expose the app directly and skip Caddy (no TLS/domain).
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build db kenbu
services:
  kenbu:
    ports:
      - "8000:8000"
